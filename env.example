# 生产：请把此文件复制为 `.env` 并收紧权限：`chmod 600 .env`

# LiteLLM（Master Key 仅运维 bootstrap 使用，不要下发给业务/不要存入 WordPress 数据库）
LITELLM_MASTER_KEY=sk-your-secure-master-key

# LiteLLM 镜像版本（企业强制锁定版本；不要用 main-latest/nightly 直接上生产）
# 版本来源建议参考官方仓库 Releases：https://github.com/BerriAI/litellm
# 文档默认版本：v1.80.11（更新日期：2026-01-13）
LITELLM_IMAGE=ghcr.io/berriai/litellm:v1.80.11-stable

# LiteLLM UI 登录（可选）
# - 若不配置，LiteLLM UI 可能采用其它登录方式（例如基于 key / SSO / 不同版本行为不同）
# - 生产建议：UI 管理面仅 VPN/堡垒机可达，并在 Nginx 侧加 BasicAuth/SSO
UI_USERNAME=
UI_PASSWORD=

# 反代对外基址（可选，但强烈建议在生产设置为 https://litellm.example.com）
# - 用于 UI/重定向在多层反代下正确生成绝对 URL，避免 https -> http 降级导致 mixed content
PROXY_BASE_URL=

# PostgreSQL
POSTGRES_USER=litellm_user
POSTGRES_PASSWORD=litellm_password_change_me
POSTGRES_DB=litellm_db

# 上游模型提供方
OPENAI_API_KEY=sk-your-openai-key
ANTHROPIC_API_KEY=sk-your-anthropic-key

# 日志
LOG_LEVEL=INFO

# 可选：仅当你确实需要浏览器跨域访问时才配置（建议白名单）
LITELLM_CORS_ORIGINS=
